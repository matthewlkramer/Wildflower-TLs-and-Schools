{
  "installCommand": "corepack enable && corepack prepare pnpm@10.14.0 --activate && pnpm -C client install --no-frozen-lockfile",
  "buildCommand": "pnpm -C client run build",
  "ignoreCommand": "node vercel-ignore-build-step.js",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
